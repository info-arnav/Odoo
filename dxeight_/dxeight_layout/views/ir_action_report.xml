<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_report_dxeight_saleorder" model="ir.actions.report">
        <field name="name">DX8 Quote/Order</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">dxeight_layout.report_saleorder_custom_sale</field>
        <field name="report_file">dxeight_layout.report_saleorder_custom_sale</field>
        <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' %
            (object.name)) or 'Order - %s' % (object.name)</field>
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="binding_type">report</field>
        <!-- <field name="groups_id" eval="[(4, ref('sale.group_proforma_sales'))]" /> -->

    </record>

    <record id="action_report_dxeight_invoice" model="ir.actions.report">
        <field name="name">DX8 Invoice</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">dxeight_layout.report_invoice_custom</field>
        <field name="report_file">dxeight_layout.report_invoice_custom</field>
        <field name="print_report_name">(object._get_report_base_filename())</field>
        <field name="attachment" />
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="binding_type">report</field>
        <!-- <field 
            name="groups_id"
            eval="[(4, ref('account.group_account_invoice')), (4, ref('account.group_account_readonly'))]" /> -->

    </record>

</odoo>